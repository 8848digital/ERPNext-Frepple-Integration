[project]
name = "frepple"
version = "0.0.1"
description = "FrePPLe ERPNext Integration"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.bench.frappe-dependencies]
frappe = "16.18.2"
erpnext = "16.18.2"
